const generateBookCover = require('./generate-cover')
const appendNovelsMetadata = require('./append-novels-metadata')
const generateMetadata = require('./generate-metadata')

module.exports = {
  generateBookCover, generateMetadata, appendNovelsMetadata
}